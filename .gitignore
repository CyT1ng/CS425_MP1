bin/
build/
*.o

# Generated logs -- never commit these. A 60 MB log file in git history is
# permanent and will make every clone on the VMs painful.
*.log
machine.*.log
logs/

# Measurement output: keep the summarized CSV under report/data, not the raw runs
report/data/raw/
*.png
!report/*.png

.DS_Store
compile_commands.json
